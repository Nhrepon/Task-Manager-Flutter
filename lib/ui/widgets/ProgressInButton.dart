import 'package:flutter/material.dart';

class ProgressInButton extends StatelessWidget {
  const ProgressInButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator(),);
  }
}

